# Daraja API Credentials
# Get these from https://developer.safaricom.co.ke/
DARAJA_CONSUMER_KEY=your_consumer_key_here
DARAJA_CONSUMER_SECRET=your_consumer_secret_here
DARAJA_SHORTCODE=174379
DARAJA_PASSKEY=your_passkey_here

# Environment (sandbox or production)
# Use 'sandbox' for testing, 'production' for live transactions
DARAJA_ENV=sandbox

# Callback Server Configuration
CALLBACK_PORT=3000
# Use 127.0.0.1 for local dev (avoids "Can't assign requested address" on macOS)
# Use 0.0.0.0 only when you need to accept external connections (e.g. Docker)
CALLBACK_HOST=127.0.0.1
# For local development, use localhost
# For production/ngrok, use your public HTTPS URL
PUBLIC_URL=http://localhost:3000
